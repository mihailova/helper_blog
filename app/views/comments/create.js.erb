<% if @comment.new_record? %>
  $('input#comment_text').effect('highlight', { color: 'red' });
<% else %>
  $('div#comments').append("<%= escape_javascript(render @comment) %>");
  $('#comment_text').val("");
  $("#add_comment").removeClass("invisible")
  $('#new-comment').addClass("invisible")
<% end %>